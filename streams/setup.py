from setuptools import find_packages, setup

setup(
    name='streams',
    packages=find_packages(include=['streams']),
    version='0.1.0',
    description='Manage streams for producing and consuming data',
    author='Eric Yeh'
)